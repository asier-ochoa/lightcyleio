{
  "name": "lightcycle",
  "module": "src/main.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "scripts": {
    "start": "DEV_ENV=true bun run --watch src/main.ts",
    "start:release": "bun run src/main.ts"
  },
  "scriptComments": {
    "start": "Builds static files and runs server with hot reloading of all files. Enables API error returns.",
    "start:release": "Builds static files, minimizes them and runs the server."
  }
}